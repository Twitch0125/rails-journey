<% unless comment.archived? %>
  <p>
    <strong>Commenter:</strong>
    <%= comment.commenter %>
  </p>
  <p>
    <strong>Comment:</strong>
    <%= comment.body %>
  </p>
  <%= link_to "Remove Comment",[comment.chore, comment], data: { turbo_method: "delete" }, class: 'link text-sm' %>
<% end %>
