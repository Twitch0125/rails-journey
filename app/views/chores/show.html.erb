<%= link_to "Edit",edit_chore_path(@chore), class: 'btn' %>
<%= link_to "Destroy", chore_path(@chore), data: { turbo_method: "delete",
    turbo_confirm: "Are you sure?" }, class: 'btn -danger' %>
<h1><%= @chore.title %></h1>
<p><%= @chore.description %></p>
<h2>
  Leave a comment:
</h2>
<%= render 'comments/form' %>
<h3>Comments</h3>
<%= render @chore.comments %>
